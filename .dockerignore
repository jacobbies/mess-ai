# Docker build context filter for backend image
# Build command: docker build -f backend/Dockerfile -t mess-ai-backend .

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments (local dev only)
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git
.git/
.gitignore

# Environment files
.env
.env.*
!.env.example

# Local data and artifacts (should come from S3 in production)
data/
*.npy
*.npz
*.pkl

# Research artifacts (not needed in backend)
research/
notebooks/
scripts/
docs/

# Cache
.cache/
*.log
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
*.ipynb
.ipynb_checkpoints/

# Build artifacts
dist/
build/
*.egg-info/

# Lock files (dependencies resolved during build)
uv.lock
backend/uv.lock

# Don't exclude these - needed for build:
# - mess/ (the library)
# - backend/ (the app)
# - pyproject.toml (library metadata)
# - README.md (library metadata)
