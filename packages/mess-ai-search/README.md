# mess-ai-search

Lightweight search/runtime distribution for MESS-AI.

This package exposes the same `mess.search` import path but installs only
search/runtime dependencies (`faiss-cpu`, `boto3`, `numpy`, `PyYAML`).
